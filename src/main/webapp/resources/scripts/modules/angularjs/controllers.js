
/* Controllers */
define(['modules/angularjs/app'], function (app) {

    "use strict";
    app.controller('ZuprTrackrMain', ['$scope', function($scope) {
        $scope.elements/* =  TODO */;
    }]);
    return app;
});